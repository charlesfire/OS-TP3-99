#include "CryptedPacket.hpp"

using namespace JC9;

/*void CryptedPacket::onSend(std::size_t& size)
{
    const void* srcData = getData();
    std::size_t srcSize = getDataSize();
}

void CryptedPacket::onReceive(const void* data, std::size_t size)
{

}*/
